DataStruct

逻辑结构
    集合(has)，线性(一对一)，树形(一对多)，图形(多对多)
物理结构
    顺序存储结构
    链式存储结构

线性表  顺序表，数组实现。 查询快，插入、删除效率低。  ArrayList

线性表  链表， 有前驱或后继指针或都有。 查询慢，插入、删除快。 LinkedList
    单链表：只有后继指针.
        MessageQueue: Message (属性 .next 指向下一个Message) 链表
            新的Message过来，头插；如果有时间delay，判断时间，中间某处插
    双向链表：不光有后继指针，还有前驱指针
        属性 .next  .previous
    单端链表：即使用链表时， 定义一个 首结点指针或尾结点指针
    双端链表：即使用链表时， 定义 首结点指针 和 尾结点指针
    双向(端)循环链表：在双向链表基础上，头指针的前驱指针=尾指针，尾指针的后继指针=头指针