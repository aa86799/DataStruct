并查集 union find
    //主要用于连接两个及以上 的 端点。 如在(社交、交通、互联网...)网络中的端点，进行合并查询。

    并查集，在一些有N个元素的集合应用问题中，我们通常是在开始时让每个元素构成一个单元素的集合.
    分离组，

    主要有两个动作
        void unionElements(int p, int q);
        void isConnected(int p, int q);


如，有 n 个元素，分成 m 个组，n/m 个元素 它们的 组编号就是相同的

    在开始时，，每个元素，都有自己的编号

    原数组 ary[i]
    编号数组 id[i]
